import 'package:flutter/material.dart';

class CustomListSeparator extends StatelessWidget {
  const CustomListSeparator({super.key});
  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 5,
    );
  }
}
