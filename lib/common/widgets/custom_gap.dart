import 'package:flutter/material.dart';

class CustomVerticalGap extends StatelessWidget {
  const CustomVerticalGap({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const SizedBox(width: 10);
  }
}
